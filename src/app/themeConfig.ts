import type { ThemeConfig } from 'antd'

const theme: ThemeConfig = {
  token: {
    fontSize: 16,
    lineHeight: 24,
    colorPrimary: '#028BBF',
    colorLink: '#028BBF',
    borderRadius: 0,
  },
}

export default theme
